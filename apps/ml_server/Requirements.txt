# ── Web Framework ──────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── NLP / Skills Extraction ───────────────────────────
# NOTE: ojd-daps-skills is installed via --no-deps (see install instructions)
# because it pins torch<2.0 which is unavailable for Python 3.12+.
# Its actual runtime dependencies are listed below individually.
spacy>=3.7.0,<3.9.0
sentence-transformers>=2.1.0,<4.0.0
torch>=2.2.0
transformers>=4.0.0
skops>=0.9.0,<0.10.0
boto3>=1.34.0
pyyaml>=6.0.1,<7.0.0
toolz>=0.12.1,<0.13.0

# ── World Bank Data Ingestion ─────────────────────────
wbgapi>=1.0.0
pandas>=2.0.0,<3.0.0

# ── Machine Learning & Explainability ─────────────────
scikit-learn>=1.3.0
lime>=0.2.0
joblib>=1.3.0

# ── Open Badges 3.0 Credential Issuance ──────────────
# Implemented locally in ml_engine/openbadges.py using Pydantic v2.

# ── Utilities ─────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
numpy>=1.26.0,<2.0.0

# ── Testing ───────────────────────────────────────────
pytest>=8.2.0,<9.0.0
httpx>=0.24.0
