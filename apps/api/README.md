# @unmapped/api

Express backend service (TypeScript) for the UNMAPPED project.

Development

Run the local dev server:

```
pnpm --filter @unmapped/api dev
```

This starts a simple Express server on port 3001 by default.
